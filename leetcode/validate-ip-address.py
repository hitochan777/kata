import re

class Solution:
    def is_ipv4(self, IP: str) -> bool:
        parts = IP.split(".")
        if len(parts) != 4:
            return False
        
        
    def is_ipv6(self, IP: str) -> bool:
        return False
        
    def validIPAddress(self, IP: str) -> str:
        
        
